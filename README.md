# jenkins-dood
Custom Jenkins image configured for Docker-Outside-of-Docker (DooD). Includes Docker CLI and essential pipeline plugins to build, test, and push images directly from Jenkins. Optimized for using the host's Docker socket.
